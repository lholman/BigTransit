interface Pipeline {
    pipelineID: string;
    name: string;
}

function create(name: string) {
    return undefined as Pipeline;
}

function fromID(_pipeline: Pipeline) {
    return undefined as Pipeline;
}